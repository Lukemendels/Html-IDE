const assert=require('assert'),fs=require('fs');const src=fs.readFileSync('local-ide.src.html','utf8');const start=src.indexOf('const SS_KEYS'),end=src.indexOf('// Pack Offline Libraries UI Event Handlers');const logic=src.slice(start,end);const localStorage={getItem:()=>null,setItem(){},removeItem(){}};const api=new Function('localStorage',logic+';return {injectStickShiftCompliance,extractGeneratedAppSkill,renderAppSkill};')(localStorage);
const close='</scr'+'ipt>';const source='---\n# {{TOOL_TITLE}}\nAPP_SKILL_SENTINEL_2026\n```json\n{"x":"${value}","html":"<b>✓</b>"}\n```\n'+close.toUpperCase();const html='<!doctype html><html><body><h1>App</h1></body></html>';const compiled=api.injectStickShiftCompliance(html,'demo-app.html',source);assert.equal((compiled.match(/APP_SKILL_SENTINEL_2026/g)||[]).length,1);assert(!compiled.includes('# Local HTML IDE Skill'));assert(compiled.includes('# Demo App'));for(const marker of ['CSS','TRIGGER','PANEL','SKILL','JS'])assert.equal((compiled.match(new RegExp('STICKSHIFT_'+marker+'_START','g'))||[]).length,1);const recovered=api.extractGeneratedAppSkill(compiled);assert(recovered.includes('APP_SKILL_SENTINEL_2026')&&recovered.toLowerCase().includes(close));const again=api.injectStickShiftCompliance(compiled,'demo-app.html',recovered);assert.equal((again.match(/APP_SKILL_SENTINEL_2026/g)||[]).length,1);assert.throws(()=>api.injectStickShiftCompliance('<html><script id="stickshift-skill" type="text/markdown">third party<'+ '/script></html>','x.html',source),/Unmarked/);assert(!html.includes('STICKSHIFT_'));console.log('app skill roundtrip tests passed');
